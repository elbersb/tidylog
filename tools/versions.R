# Versions of dplyr/tidyr used to generate wrapper documentation.
# Developers must have these exact versions installed to regenerate wrappers
# (or update these constants to match their installed versions).
# Pinned for reproducibility, but does NOT affect runtime compatibility —
# users may have any version installed.
WRAPPER_DOC_VERSIONS <- c(
    dplyr="1.2.1",
    tidyr="1.3.2"
)
